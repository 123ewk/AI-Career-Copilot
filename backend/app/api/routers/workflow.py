"""工作流路由（占位）

未来实现：
- POST /: 创建工作流
- GET /{workflow_id}: 工作流详情
- POST /{workflow_id}/start: 启动工作流
- POST /{workflow_id}/pause: 暂停工作流
"""
from fastapi import APIRouter

# 占位 router：模块路由的真实实现由后续 PR 补齐
router = APIRouter()
